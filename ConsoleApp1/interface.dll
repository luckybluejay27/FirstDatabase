public interface IArtDatabase
{
    void Insert (Resources Data);
    void Delete (Resources Data);
    void Update (Resources Data);
    Resources Find (int ID); 
    IEnumerable<Resources> FindBy
        (
            int NumberTag,
            string FileName,
            string FileType,
            string PathLink,
            array DateCreated,
            array CurrentVersion,
            array FirstVersion,
            string Author,
            string Editor,
            string Description,
        );
}